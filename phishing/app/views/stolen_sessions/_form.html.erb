<%= form_for(@stolen_session) do |f| %>
  <% if @stolen_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stolen_session.errors.count, "error") %> prohibited this stolen_session from being saved:</h2>

      <ul>
      <% @stolen_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
