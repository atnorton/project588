<nav class="navbar navbar-default" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Auth Test</a>
    </div>
    <% if signed_in? %>
      <div class="navbar-text navbar-right">
          <%= button_to "Log Out", sessions_path, method: "delete", class: "btn btn-default btn-xs" %>
      </div>
      <p class="navbar-text navbar-right">logged in as <i><%= (current_user!=nil)? current_user.email : 'None' %></i>
      </p>
    <% end %>
  </div>
</nav>
