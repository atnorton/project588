<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div class="alert alert-danger">
      <div>
        <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>
      </div>
      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :handle %><br>
    <%= f.text_field :handle, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :pending_email, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit class: "btn btn-primary btn-lg active" %>
  </div>
<% end %>
